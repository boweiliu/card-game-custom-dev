/**
 * Concept: use plain olds divs (with data fields) as our source of truth for data model on the frontend.
 * Usually people do this with in-memory javascript models, but because we are using jquery and we have the opportunity
 * to open up the "hood" so to speak and allow players/devs to directly edit model state, we can use this to our advantage.
 */

export class DomRepo {
  // TODO: implement
  // For instance - we should manage the protocard syncable state in here
}
